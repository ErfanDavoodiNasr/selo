{
  "name": "selo-messenger",
  "private": true,
  "version": "1.0.0",
  "description": "React/Vite build tooling for committed SELO browser assets",
  "scripts": {
    "build": "vite build && node scripts/sanitize-build-assets.js",
    "dev": "vite --host 127.0.0.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.2",
    "esbuild": "^0.28.0",
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "vite": "^8.0.14"
  }
}
